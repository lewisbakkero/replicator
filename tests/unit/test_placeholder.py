"""Placeholder unit test to verify pytest collection works against the scaffold.

Subsequent tasks replace this with real unit tests for the catalog, manifest,
config, retry, hashing, and duplicates modules.
"""


def test_placeholder():
    """Smoke check: pytest collects and runs a test in tests/unit/."""
    assert True
